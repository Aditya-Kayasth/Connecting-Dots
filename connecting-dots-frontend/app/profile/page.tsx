'use client'
import { useMemo, useState } from 'react'
import ReviewsList from '@/components/reviews-list'
function getInitials(name: string) { const words = name.trim().split(/\s+/).filter(Boolean); if (!words.length) return 'CD'; if (words.length === 1) return `${words[0][0]}${words[0][0]}`.toUpperCase(); return `${words[0][0]}${words[words.length - 1][0]}`.toUpperCase() }
export default function ProfilePage() { const [tab, setTab] = useState('Profile Details'); const [name, setName] = useState('Maya Chen Singh'); const [saved, setSaved] = useState(false); const initials = useMemo(() => getInitials(name), [name]); const tabs = ['Profile Details','Account Settings','Reviews & Activity']; return <main className="page-shell"><div className="profile-content"><a className="back-link" href="/">← Back to explore</a><section className="profile-hero settings-hero"><div className="profile-avatar dynamic-avatar">{initials}</div><div><span className="eyebrow">Technical contributor</span><h1>Profile settings</h1><p className="muted">Keep your public profile current so the right missions can find you.</p></div></section><div className="settings-tabs" role="tablist">{tabs.map((item) => <button key={item} role="tab" aria-selected={tab === item} className={tab === item ? 'settings-tab active' : 'settings-tab'} onClick={() => setTab(item)}>{item}</button>)}</div>{tab === 'Profile Details' && <section className="settings-panel"><div className="settings-heading"><div><span className="eyebrow">Public profile</span><h2>Profile details</h2></div><span className="status verified">TRUSTED CONTRIBUTOR</span></div><div className="settings-grid"><label>Full name<input className="profile-input" value={name} onChange={(e) => { setName(e.target.value); setSaved(false) }} /></label><label>Title & location<input className="profile-input" defaultValue="Product designer & frontend engineer · Nairobi, Kenya" /></label><label className="wide-field">Skills summary<textarea className="profile-input" defaultValue="Product design, frontend engineering, accessibility, research, and design systems." /></label><label>Portfolio URL<input className="profile-input" type="url" defaultValue="https://github.com/mayachen" /></label><label>Preferred language<select className="profile-input" defaultValue="English"><option>English</option><option>Hindi</option><option>Marathi</option><option>Swahili</option></select></label></div><button className="primary-button" onClick={() => setSaved(true)}>{saved ? 'Profile saved' : 'Save profile'}</button></section>}{tab === 'Account Settings' && <section className="settings-panel"><div className="settings-heading"><div><span className="eyebrow">Private access</span><h2>Account settings</h2></div></div><div className="settings-grid"><label>Email address<input className="profile-input" type="email" defaultValue="maya.chen@example.com" /></label><label>Current password<input className="profile-input" type="password" placeholder="••••••••" /></label><label>New password<input className="profile-input" type="password" placeholder="At least 8 characters" /></label></div><div className="preference-list"><label><input type="checkbox" defaultChecked /> Email me when an NGO responds to my application</label><label><input type="checkbox" defaultChecked /> Send a weekly digest of new problems</label></div><button className="primary-button" onClick={() => setSaved(true)}>{saved ? 'Settings saved' : 'Save settings'}</button></section>}{tab === 'Reviews & Activity' && <section className="settings-panel"><div className="settings-heading"><div><span className="eyebrow">Reputation</span><h2>Reviews & activity</h2></div><div className="activity-score">4.9 <span>/ 5</span></div></div><div className="completed-list"><article><strong>Community Health Data Portal</strong><span>Completed · 2026</span></article><article><strong>Rural School Attendance Tracking</strong><span>Completed · 2025</span></article></div><ReviewsList /></section>}</div></main> }
